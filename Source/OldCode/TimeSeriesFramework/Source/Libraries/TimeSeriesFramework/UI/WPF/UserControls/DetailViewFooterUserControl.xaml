<UserControl x:Class="TimeSeriesFramework.UI.UserControls.DetailViewFooterUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="ButtonDelete" Command="{Binding DeleteCommand}" Content="Delete" Margin="2" />
        <Button x:Name="ButtonClear" Command="{Binding ClearCommand}" Content="Clear" Margin="2"/>
        <Button x:Name="ButtonSave" Command="{Binding SaveCommand}" IsEnabled="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=SaveEnabled}" Content="Save" Margin="2"/>
    </StackPanel>
    
</UserControl>
